# Test data fetched by scripts/bootstrap.sh
/data/

# pyogctest drops its raw report here before run-suite.sh files it away
/teamengine.xml

# Suite output
/reports/

# Local overrides
/.env
/docker-compose.override.yml

# Per global instructions: prompt log is kept locally, never committed.
/PROMPT.log

__pycache__/
*.pyc
